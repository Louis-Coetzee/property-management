================================================================================
 FIND ACCOMMODATION → PROPERTY MANAGEMENT MERGE PLAN
 Source: /Findaccommodation/Findaccommodation
 Target: /PopertyM
 Created: 2026-06-09
 Updated: 2026-06-09
================================================================================

STRATEGY: Copy FROM Find Accommodation INTO Property Management.
Find Accommodation becomes the main site for NEXT_PUBLIC_PLATFORM_DOMAIN.
Property Management multi-tenant system stays 100% intact.
Fresh start — no data migration needed.

================================================================================
 STAGE 1: CONVEX SCHEMA + FUNCTIONS + PLATFORM PAGES
 Status: ✅ COMPLETE
================================================================================

COMPLETED:
  ✅ Schema tables added to convex/schema.ts:
     - listings, listingAvailability, accommodationInquiries,
       inquiryNotes, inquiryEmails, alerts, analytics,
       savedItems, newsletterSubscriptions, location

  ✅ Convex functions created:
     - convex/faListings.ts (company-scoped CRUD)
     - convex/accommodationInquiries.ts (booking inquiries)
     - convex/analytics.ts, locations.ts, newsletter.ts
     - convex/savedItems.ts, availability.ts, listings.ts
     - convex/alerts.ts, contact.ts

  ✅ Platform pages created at app/ root level:
     - app/contact/page.tsx + app/api/contact-form/route.ts
     - app/advertise/page.tsx
     - app/about/page.tsx
     - app/terms/page.tsx
     - app/privacy/page.tsx
     - app/listings/page.tsx + app/listings/[id]/page.tsx
     - app/saved-items/page.tsx

  ✅ Root-level components:
     - components/platform/PlatformNavbar.tsx
     - components/platform/PlatformFooter.tsx
     - components/platform/RootConvexProvider.tsx

  ✅ app/layout.tsx updated with RootConvexProvider + metadata

  ✅ AccommodationLandingPage.tsx auth links fixed (all use authBase)

  ✅ Company listings management page:
     - app/[domain]/companies/[companyId]/listings/page.tsx
     - app/[domain]/companies/[companyId]/manage/page.tsx updated

================================================================================
 STAGE 2: ESSENTIAL [DOMAIN] COMPONENTS
 Status: ✅ COMPLETE
================================================================================

All copied to components/ and components/ui/:

UI COMPONENTS (23 files copied from FA):
  ✅ card.tsx, input.tsx, label.tsx, select.tsx, badge.tsx, slider.tsx
  ✅ checkbox.tsx, form.tsx, textarea.tsx, switch.tsx, separator.tsx
  ✅ popover.tsx, radio-group.tsx, alert-dialog.tsx, dropdown-menu.tsx
  ✅ login-modal.tsx, multi-media-picker.tsx, media-picker.tsx
  ✅ Installed @radix-ui/* packages (14 packages)
  ✅ Removed FA shadcn toast system — using PM's use-toast.tsx (react-hot-toast)
  ✅ Added DialogDescription to dialog.tsx
  ✅ Added getOptimizedImageProps to lib/cloudflare-images.ts

LISTING COMPONENTS (5 files):
  ✅ listing-card.tsx (main listing card)
  ✅ listing-filters/ (4 files: index.ts, index.tsx, listing-filters.tsx, listing-filters-modal.tsx)
  ✅ add-listing-form.tsx (auth import fixed to PM's AuthProvider)
  ✅ edit-listing-form.tsx (auth import fixed to PM's AuthProvider)
  ✅ user-listing-card.tsx
  ✅ payment-gateway-selector.tsx (needs api.sites — deferred to Stage 4)

FIXES APPLIED:
  ✅ Auth imports: @/app/context/AuthContext → @/app/[domain]/AuthProvider
  ✅ user.role casts: (user as any)?.role (PM User type has no role field)
  ✅ Implicit any types fixed in filters, forms, toast
  ✅ convex/alerts.ts: ctx.db.get → ctx.runQuery (action context)
  ✅ companies/listings page: added missing showDeleteModal state

PENDING (Convex deploy required):
  ⏳ api.faListings, api.listings, api.alerts, api.accommodationInquiries,
     api.contact, api.sites — all need npx convex deploy to generate types

================================================================================
 CONVEX DEPLOY
 Status: ✅ COMPLETE
================================================================================

  ✅ npx convex deploy --yes — all functions deployed to dashing-gull-105
  ✅ Schema tables pushed: listings, alerts, accommodationInquiries, etc.
  ✅ TypeScript bindings generated
  ✅ Circular type references fixed (explicit return types on actions)
  ✅ esbuild warning fixed (bare return → parenthesized return)
  ✅ payment-gateway-selector adapted (api.sites → DEFAULT_GATEWAYS)
  ✅ TypeScript typecheck: 0 errors

================================================================================
 STAGE 3: PAGE ROUTES + BOOKING FLOW
 Status: ✅ COMPLETE
================================================================================

ROOT-LEVEL PAGES (not under [domain]):
  ✅ app/listings/[id]/availability/page.tsx — calendar availability management
  ✅ app/bookings/new/page.tsx — multi-step booking (dates → guest info → confirm)
  ✅ app/bookings-requests/page.tsx — host-facing booking inquiry management
  ✅ app/bookings/[bookingCode]/page.tsx — booking detail + PDF download
  ✅ app/payment/booking/checkout/page.tsx — PayFast/Paygate checkout
  ✅ app/payment/booking/success/page.tsx — booking success + confirmation
  ✅ app/payment/booking/cancel/page.tsx — payment cancelled page
  ✅ app/saved-items/page.tsx — saved listings with search/sort/unsave

CONVEX:
  ✅ New accommodationBookings table in schema (separate from appointment bookings)
  ✅ convex/accommodationBookings.ts — createBooking, getBookingByCode,
     getUserBookings, getListingBookings, updateBookingStatus, cancelBooking

INFRASTRUCTURE FIXES:
  ✅ components/platform/RootAuthProvider.tsx — auth for root-level pages
  ✅ app/layout.tsx updated with RootAuthProvider wrapper
  ✅ Root-level pages use useRootAuth instead of useAuth
  ✅ bookings/new wrapped with Suspense (useSearchParams requirement)
  ✅ pnpm-lock.yaml updated for Vercel deployment

================================================================================
 STAGE 4A: AUTH PAGES FOR PLATFORM DOMAIN
 Status: ✅ COMPLETE
================================================================================

Root-level auth pages using PM's existing auth API routes:
  ✅ app/auth/login/page.tsx — login with sessionToken in localStorage
  ✅ app/auth/register/page.tsx — registration with existing user detection
  ✅ app/auth/forgot-password/page.tsx — password reset email
  ✅ app/auth/reset-password/page.tsx — password reset form (Convex action)
  ✅ app/auth/verify-email/page.tsx — email verification (Convex mutation)
  ✅ app/auth/resend-verification/page.tsx — resend verification email

All pages:
  - Use getPlatformDomain() for domain parameter
  - Use PM's auth API routes (/api/auth/*)
  - Use AuthLayoutWrapper, AuthInput, AuthButton, AuthAlert from @/components/auth
  - Use react-hot-toast (PM's toast system)
  - Wrapped in Suspense boundaries for useSearchParams()
  - proxy.ts updated to exclude /auth from [domain] rewrite

================================================================================
 STAGE 4B: API ROUTES + LIB
 Status: ⏳ PENDING
================================================================================

API ROUTES (FA-specific, not already in PM):
  - app/api/paygate/ (Paygate payment integration — NEW for PM)
  - app/api/send-booking-email/ (booking email notifications)
  - app/api/send-admin-booking-email/ (admin booking notifications)
  - app/api/send-owner-booking-email/ (owner booking notifications)
  - app/api/send-inquiry-email/ (inquiry email)
  - app/api/send-inquiry-confirmation-email/ (inquiry confirmation)
  - app/api/send-inquiry-notification-email/ (inquiry notification)
  - app/api/send-payment-details-email/ (payment details)
  - app/api/send-alert-email/ (alert notifications)
  - app/api/send-alert-creation-email/ (alert creation confirmation)
  - app/api/analytics/ (analytics tracking endpoints)

Already done:
  ✅ app/api/contact-form/route.ts
  ✅ lib/paygate-utils.ts (Paygate integration)
  ✅ lib/payment-utils.ts (payment utilities)
  ✅ lib/payfast-config.ts (PayFast config)
  ✅ convex/sites.ts (site config functions)
  ✅ sites table in schema
  ✅ react-day-picker installed
  ✅ PM's lib/email.ts has full Resend-based email system

LIB FILES still to copy:
  - lib/payment-confirmation-templates.ts (email templates)
  - lib/pdf-utils.ts (PDF generation)
  - lib/email-components.ts (email component helpers)
  - lib/email-templates.ts (email templates — check if PM's covers it)

SKIP (PM already has):
  - lib/auth.ts, lib/utils.ts, lib/convex.ts

================================================================================
 STAGE 5: FINAL INTEGRATION + CLEANUP
 Status: ⏳ PENDING
================================================================================

5A. Copy remaining public assets (icons, manifest, brochure)
5B. Update package.json if any new dependencies needed
5C. Ensure globals.css has FA styles
5D. Verify Paygate integration works
5E. Test complete flow: browse → search → book → pay
5F. Update any hardcoded URLs

================================================================================
 STAGE 6: VERIFY BUILD
 Status: ⏳ PENDING
================================================================================

6A. Run: npm run build
6B. Fix any TypeScript errors
6C. Fix any import errors
6D. Fix any missing dependencies
6E. Confirm no PM functionality is broken
6F. Confirm FA functionality works on platform domain

================================================================================
 CONFLICT RESOLUTION NOTES
================================================================================

1. CONVEX SCHEMA TABLES:
   - `inquiries`: PM has form-based, FA has booking-based → SEPARATE tables
     (PM: inquiries, FA: accommodationInquiries)
   - `pages`: PM has website pages → REUSE PM's
   - `sections`: Both have similar → REUSE PM's
   - `bookings`: PM has appointment bookings → FA needs separate (future)
   - `mediaLibrary`: REUSE PM's (more feature-rich)
   - `mediaCategories`: REUSE PM's

2. CONVEX FUNCTIONS:
   - `auth.ts`: KEEP PM's
   - `users.ts`: KEEP PM's
   - `admin.ts`: KEEP PM's, extend if needed
   - `media.ts`: KEEP PM's

3. COMPONENTS:
   - `auth/`: KEEP PM's auth components
   - `ui/`: MERGE — add FA-specific UI components
   - `providers/`: KEEP PM's providers

4. API ROUTES:
   - `auth/`: KEEP PM's
   - `payfast/`: EXTEND PM's with FA config
   - `user/`: KEEP PM's

5. ROUTING:
   - Platform pages (contact, about, advertise, terms, privacy, listings):
     → Root app/ level (not under [domain])
   - Auth pages (login, register, forgot-password, reset-password, verify-email, resend-verification):
     → Root app/auth/ level (not under [domain])
   - Company-specific pages (dashboard, bookings, inquiries):
     → Under app/[domain]/ routes
   - proxy.ts excludes /auth from [domain] rewrite (like /api/, /_next/, etc.)

================================================================================
